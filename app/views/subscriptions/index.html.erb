   <h1>Subscriptions</h1>
        <div>
          <% @subscriptions.each do |subscription| %>
            <% binding.pry %>
            <h2>Level: <%= subscription.level %></h2>
            <p>Description: <%= subscription.description %></p>
            <p>Rate: <%=subscription.rate%>
           <% end %>
        </div>
