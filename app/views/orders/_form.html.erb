<%= form_for order do |f| %>
<%= f.collection_select :subscription, Subscription.all, :id, :level %>
<%= f.hidden_field :user_id %>
<%= f.label: :first_name%>
<%= f.first_name %>
<%= f.label: :last_name%>
<%= f.last_name %>
<%= f.label: :email%>
<%= f.email
<%= f.submit %>
<% end %>